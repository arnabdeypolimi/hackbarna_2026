"""Ambient scenes: relaxing looping videos the viewer asks the avatar for.

Only the vocabulary lives here. The video itself is made in the TV app
(frontend/src/ambient) from a fal Director session through the theme agent's
proxy; this backend never touches media.
"""
