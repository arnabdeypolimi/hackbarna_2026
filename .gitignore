.env
.venv/
__pycache__/
*.py[cod]
.pytest_cache/
.serena/
.claude/.headroom_wrap_*
claude-setup.lock.json
.worktrees/

# Phase 2 — local data, never committed
data/
# ...but the TV app's static dataset ships with it.
!frontend/public/data/
.DS_Store

# Playwright rehearsal harness — deps and run output are local
tools/rehearsal/node_modules/
tools/rehearsal/last-run.json
